cmake_minimum_required(VERSION 2.8)
project(lerImagem)
find_package( OpenMP )
find_package( OpenCV REQUIRED )
add_executable( lerImagem nomearquivo.cpp )
target_link_libraries( lerImagem ${OpenCV_LIBS} )
if(OpenMP_CXX_FOUND)
    target_link_libraries(lerImagem PUBLIC OpenMP::OpenMP_CXX)
endif()
